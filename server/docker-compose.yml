services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=ADMIN
      - POSTGRES_PASSWORD=ADMIN
      - POSTGRES_DB=NaradMuni
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
